name: Keep Render Awake (fallback)
on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS --max-time 90 --retry 3 --retry-delay 15 https://blog-rest-api.onrender.com/api/v1/posts > /dev/null
