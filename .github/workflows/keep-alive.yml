name: Keep Render Awake
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -sS --max-time 90 --retry 3 --retry-delay 15 https://blog-backend-a40u.onrender.com/api/v1/posts > /dev/null
